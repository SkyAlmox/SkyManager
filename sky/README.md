## Run Locally

**Prerequisites:**

- Node.js
- ngrok

1. Install dependencies:

   `npm install`

   `npm install -g ngrok`
2. Run the app:
   `npm run dev`
3. Run the ngrok:

   `ngrok http 3000`

   `http://127.0.0.1:4040`
